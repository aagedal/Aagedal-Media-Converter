# 4.5 MPVKit CoreAudio package validation — 2026-09-25

The verified GPL libmpv candidate was published to the public
[MPVKit CoreAudio candidate prerelease](https://github.com/aagedal/MPVKit/releases/tag/aagedal-1.0.0-n8.1.2-coreaudio.1)
after the user explicitly authorized uploading its binary and matching source.
The exact MPVKit revision is `2d80ed953c8bdb79c46241ceaaaa29d6f1209c63`.
Its `Libmpv-GPL` target points at the new tag; all other binary target URLs
remain unchanged. Xcode resolved precisely that revision.

The published files were downloaded and hashed again:

| Asset | SHA-256 |
| --- | --- |
| `Libmpv-GPL.xcframework.zip` | `abd855457c4783efdb9a5827faa0f1439392cba32150550b1694fe79e6fb3856` |
| `mpv-coreaudio-4.5-source.tar.gz` | `4ee0318085084c9bd8f4e8d3daea0ca5748f24236af504eab92158ceeec1c204` |

The source archive's `audio/out/ao_coreaudio.c` hash matches the retained build
evidence. Its local copy is
`build/attribution/mpvkit/mpv-coreaudio-4.5-source.tar.gz`.
This app's project and package attribution point to the new revision. The source
manifest uses the patched mpv source, a snapshot of the new MPVKit revision,
and the candidate's build configuration and patch. The previous 4.4 provenance
files remain historical.

Local validation against the published package:

- `swift package dump-package` parsed the updated MPVKit manifest.
- `xcodebuild -resolvePackageDependencies` selected MPVKit `2d80ed9`.
- An unsigned Debug app build succeeded; log:
  `/private/tmp/amc-45-coreaudio-build.log`.
- All 15 `MPVPreviewObservationTests` passed, including four real CoreAudio
  playback and teardown cycles; log and result bundle:
  `/private/tmp/amc-45-coreaudio-published-tests.log` and `.xcresult`.
- An unsigned Release archive succeeded at
  `/private/tmp/amc-45-coreaudio-unsigned.xcarchive`; log:
  `/private/tmp/amc-45-coreaudio-archive.log`.
- Static bundle validation passed for 45 arm64 Mach-O images, bundled library
  reachability, and 12 packaged license notices. Report:
  `/private/tmp/amc-45-coreaudio-bundle-report.json`.
- `scripts/package-attribution-sources.py --check` verified 89 archives;
  `scripts/bundled-dependency-manifest.py --check --require-complete-licenses`
  and `scripts/generate-mpvkit-notices.py --check` passed. A source companion
  was assembled at `/private/tmp/amc-4.5-coreaudio-sources-candidate.tar`.

The focused MPV sequence replay UI runner again stopped making progress before
starting any test method. It was interrupted after about two minutes; no UI
assertion ran. The earlier interactive replay check with the same candidate
binary passed, as documented in
`4.5-mpv-coreaudio-runtime-validation-2026-09-25.md`.

This validates the published package and the unsigned app archive. The original
failure signature still needs a targeted AudioUnit initialization failure and
hotplug/device-change exercise on an affected macOS system. A signed Release
bundle and the full release process also remain to be checked before marking
the MPV crash blocker closed.
