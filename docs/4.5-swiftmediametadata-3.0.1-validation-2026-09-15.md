# 4.5 SwiftMediaMetadata 3.0.1 validation — 2026-09-15

## Scope

This continuation updates the 4.5 application from SwiftMediaMetadata 3.0.0 to
the released 3.0.1 tag. The package resolver selected revision
`8662054299a3e13c49c65f74c564360559d1bf7f`.

The upstream application-relevant change makes Sony RTMD track discovery skip
top-level `mdat` payloads instead of copying complete audio or video payloads into
memory. Structural box parsing and file-absolute RTMD sample offsets retain their
existing behavior. The update also contains upstream test-resource and CLI release
packaging fixes; it does not change the library license set.

## Attribution and retained source

- The Xcode package requirement and `Package.resolved` now select 3.0.1.
- `PackageAttributions.json`, `BundledDependencies.json`, and the GeoNames notice
  bind the package to revision
  `8662054299a3e13c49c65f74c564360559d1bf7f`.
- The reviewed tag source is retained as
  `build/attribution/swiftmediametadata-3.0.1-source.tar.gz`: **1,487,639 bytes**,
  SHA-256
  `c8eeb15ce37a4a3a2db3569423dc020866aed39a7807b4c1a37068b8a16f3aa2`.
- The decompressed tag archive and `git archive` for the resolved revision have
  the same SHA-256,
  `761cd0161495c02743694f1ccb876d4bb39dd2c5546961a461f71deddad8072f`.
- The upstream `LICENSE` is byte-identical to the existing collected
  SwiftMediaMetadata notice. The GeoNames acknowledgement remains present and its
  version/revision preamble was refreshed.
- Strict retained-source and complete-license verification passes for all 89
  recorded source archives and the complete bundled dependency inventory.

## Automated results

- Upstream `RTMDReaderTests`: **4 passed**, 0 failed.
- Full app XCTest unit target: **931 passed**, 0 failed, 0 skipped.
- Release-script suite: **74 passed**.
- Release configuration build: passed with SwiftMediaMetadata 3.0.1 and the
  embedded MCP helper.
- Static Release bundle audit: **45 arm64 Mach-O images** passed executable,
  bundled-library, and Hardened Runtime checks; **12 packaged notices** matched
  the dependency manifest.
- `git diff --check`: passed before recording this document.

The full app XCTest result retains four quality-of-service priority-inversion
runtime warnings in the broad existing test target. They are unchanged from the
preceding 4.5 validation and are not attributed to this dependency update.

## Beta assessment

The dependency update is complete and removes one release-maintenance item; it
does not change the remaining functional beta gates. Those remain named-client
MCP validation, packaged cold launch/reconnect/restart, lost-access behavior, the
six-preset output/playback matrix, the App Sandbox scope decision, and Developer
ID archive/notarization plus clean-install validation.
