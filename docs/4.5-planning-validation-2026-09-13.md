# 4.5 conversion-planning validation — 2026-09-13

## Scope

- Continued the shared job boundary on `codex/release-4.5` after immutable preset
  settings capture.
- Added Codable plan IDs and versioned plans containing the accepted request,
  expiry, source identities, deterministic per-source output URLs, and warnings.
- Captured the filename counter start so custom `{counter}` templates do not read
  mutable preferences after request creation. Older schema-1 snapshots without
  that field still decode with the established default.
- Added source identity checks using the standardized URL, file size,
  modification date, and filesystem resource identifier where available.
- Added stable errors for unknown or expired plans, missing or changed sources,
  unsupported source extensions, duplicate planned outputs, and output
  collisions.
- Added submit-time source and output revalidation plus actor-owned output
  reservations. Reservations are released on terminal job transitions.
- Kept requester-scoped idempotency authoritative across a retry that creates a
  new equivalent plan, so its own accepted output reservation does not turn into
  a false collision.

## Results

- Focused application job contract and planning suite: **21 passed**, no failures
  or skips.
- Full unit target: **890 passed**, no failures or skips.
- The full run reported four existing QoS priority-inversion runtime warnings in
  `Aagedal_Media_Converter_Tests.swift`.
- The app and test targets compile under Swift 6 strict concurrency.
- `git diff --check` passes.

## Boundaries still open

The planner and reservations are currently in-memory. Job/plan persistence,
retention cleanup, approved-folder authorization, conversion execution from the
captured settings, UI/App Intent routing, and the local MCP transport remain
subsequent work. No signed-helper feasibility or supported-client decision is
claimed by this increment.
