# MPV candidate archive link validation — 2026-09-23

The read-only candidate verifier now checks targets of ZIP symlinks as well as
member names. It rejects absolute, parent-traversing, ambiguous, backslash, and
oversized targets before comparing archived binaries. The retained XCFramework
uses three relative framework symlinks, which pass this check. No archive is
extracted by the verifier.

Validation:

```sh
python3 -m unittest discover -s scripts/tests -p 'test_mpv_candidate.py'
python3 -m unittest discover -s scripts/tests -p 'test_mpv_gpl_release.py'
python3 scripts/verify-mpv-candidate.py \
  --candidate /private/tmp/amc-mpv-coreaudio-20260922-d \
  --evidence docs/dependency-patches/mpv-coreaudio-build-2026-09-22.json
```

All 20 candidate tests and two GPL preparation tests pass. The retained candidate
passes the updated audit with seven matching artifact hashes, correct framework
and thin architectures, matched archived binaries, and CoreAudio source/patch
correspondence. The prepared `Libmpv-GPL.xcframework.zip` is byte-identical to
the verified candidate ZIP. Both SHA-256 and `swift package compute-checksum`
report `abd855457c4783efdb9a5827faa0f1439392cba32150550b1694fe79e6fb3856`.

The asset remains under `/private/tmp` and is unpublished. A durable versioned
MPVKit release, package revision and exact app pin, complete provenance and
notice review, release-bundle checks, and affected-macOS CoreAudio and playback
testing remain required before publication. No shipped binary or package pin
changed here.
