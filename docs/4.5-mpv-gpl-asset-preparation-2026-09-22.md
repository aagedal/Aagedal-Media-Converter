# MPV CoreAudio GPL asset preparation — 2026-09-22

The retained patched libmpv candidate was built with Meson's GPL option set to
`true` for both arm64 and x86_64. Its archive is named
`Libmpv.xcframework.zip`, while the app's selected `MPVKit-GPL` product uses
the `Libmpv-GPL` binary target. The XCFramework inside both archive variants is
named `Libmpv.xcframework`, so the release asset needs a GPL file name, not a
changed framework/module name.

`scripts/prepare-mpv-gpl-release.py` checks the original candidate, both actual
Meson configurations, and archive correspondence before copying the ZIP to
`/private/tmp/amc-mpv-gpl-release-20260922/Libmpv-GPL.xcframework.zip`.
The resulting SHA-256 and `swift package compute-checksum` both report
`abd855457c4783efdb9a5827faa0f1439392cba32150550b1694fe79e6fb3856`.
The original and prepared ZIP bytes are identical. Preparation metadata is in
the temporary `release-preparation.json`.

Validation: all 17 focused Python tests passed, including rejecting a missing
or disabled GPL option. The retained candidate's seven artifact hashes, actual
architecture checks, and archived binary comparisons passed again. No package
revision, resolved pin, or shipped app binary changed.

The prepared asset still needs durable versioned publication, a corresponding
MPVKit package commit and app pin, complete source/dependency provenance and
notices, Release-bundle checks, and CoreAudio failure/device-change/replay/
teardown testing on the affected macOS version. Temporary storage alone cannot
serve as a release dependency.
