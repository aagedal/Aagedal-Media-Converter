# 4.5 settings-capture validation — 2026-09-12

## Scope

- Continued milestone 2 on `codex/release-4.5` after the shared job-contract
  foundation.
- Added immutable, Codable preset snapshots for H.264, HEVC, ProRes, Proxy,
  Audio Only, and Stream Copy.
- Captured stable container, encoder, profile, audio codec, rate-control,
  resolution, metadata, subtitle, and filename values without exposing localized
  UI labels as protocol identifiers.
- Included the snapshot in request serialization, validation, and idempotency
  payload comparison.

## Results

- Focused application job-contract suite: **14 passed**, no failures.
- Full unit target: **882 passed**, no failures or skips. Xcode reported four QoS
  priority-inversion runtime warnings in the existing broad test file.
- The app and test targets compile under Swift 6 strict concurrency.
- `git diff --check` passes.

## Boundaries still open

The live UI and App Intent paths do not execute from these snapshots yet. Output
planning, collision reservation, job persistence, the conversion executor bridge,
and the local transport remain subsequent work. No feasibility decision for a
bundled MCP helper or helper-to-app IPC is claimed by this increment.
