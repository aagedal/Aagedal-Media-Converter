# 4.5 Swift package pin validation — 2026-09-23

The Xcode 27 workspace repeatedly removed its `Package.resolved` file, including
when `-onlyUsePackageVersionsFromResolvedFile` was requested. The three project
package requirements now use exact Git revisions instead: MPVKit
`230c3174f1515898f24599147ad61c2a277d0dc2`, Sparkle
`ac2def288cbff5cfc7df3ffef6abdf45b72bcb0a`, and SwiftMediaMetadata
`8662054299a3e13c49c65f74c564360559d1bf7f`. The strict bundled-dependency
manifest check reads those requirements from `project.pbxproj` and compares every
URL and revision against `PackageAttributions.json`. It rejects moving
requirements, changed revisions, and packages without attribution. The release
script no longer depends on Xcode retaining its generated workspace lockfile.

## Validation

- All 20 dependency-manifest tests and 99 release-script tests passed, including
  the new moving-requirement case. `scripts/bundled-dependency-manifest.py --check --require-complete-licenses`
  passed without `Package.resolved`.
- `plutil -lint` passed for `project.pbxproj`.
- `xcodebuild -resolvePackageDependencies` checked out exactly those three
  revisions with isolated DerivedData.
- An unsigned Debug app build passed with `-skipPackageUpdates` and isolated
  DerivedData. Log: `/private/tmp/amc-45-pins-build.log`.
- An unsigned Release archive passed with `-skipPackageUpdates` and the exact
  project revisions. Log: `/private/tmp/amc-45-sept23-archive.log`; archive:
  `/private/tmp/amc-45-sept23-unsigned.xcarchive`. Static bundle validation
  verified 45 arm64 Mach-O images and 12 packaged license notices. Report:
  `/private/tmp/amc-45-sept23-archive-bundle-report.json`.

The initial sandboxed build could not write Xcode and Swift package caches;
the approved retry passed. This checks local package resolution and an unsigned
archive. Developer ID signing, notarization, and distribution validation remain
separate gates.
