# 4.5 generated camera-card review UI continuation

A Debug-only UI fixture now copies a generated media clip into a two-file camera
card, scans that directory through `CameraCardScanner`, and opens the ordinary
camera-card import sheet. A focused English/Norwegian UI test checks the card
name, review proposal, imported group, both file rows, and sequence preview.
Stable accessibility identifiers address the card name and review controls.

The focused `xcodebuild test` compiled the app and UI test runner with exact
package pins using an isolated DerivedData directory. Xcode then reported:
`The test runner hung before establishing connection.` No test method began,
so this is compile evidence only. Log:
`/private/tmp/amc-45-card-ui.log`; result bundle:
`/private/tmp/amc-45-card-ui-dd/Logs/Test/Test-Aagedal Media Converter-2026.09.23_20-54-36-+0200.xcresult`.
An incremental `build-for-testing` after the final assertions passed; log:
`/private/tmp/amc-45-card-build-for-testing.log`. The 1,791-entry Norwegian
catalog audit and `git diff --check` also passed.
The first sandboxed attempts stopped earlier because Xcode could not write its
compiler caches; the later build ran with the required cache access.

Run the focused UI test in an interactive test session before treating the
camera-card review flow as validated. Physical card, MXF, spanned-recording,
output, and playback checks remain separate release work.
