# 4.5 shared-boundary foundation validation — 2026-09-12

## Scope

- Branch: `codex/release-4.5`, based on `89e5f2e`.
- Development version: **4.5.0 (577)**.
- Added the transport-neutral request, preset identifier, job record, stable error
  code, lifecycle, cancellation, restart, and idempotency foundation described in
  the 4.5 release plan.
- Corrected the 4.4-specific IMF restriction text without enabling IMF export.

## Results

- Full unit target: **879 passed**, no failures.
- Focused job-contract coverage: **10 passed**.
- Focused conversion dependency/IMF preflight coverage: **12 passed**.
- Release-script suite: **74 passed**.
- Bundled dependency manifest freshness: passed.
- Norwegian localization audit: passed with 1,562 catalog entries and the same 15
  intentional untranslated format/command tokens.
- Built Debug bundle reports **4.5.0 (577)**.
- Xcode project and string catalog parse successfully; `git diff --check` passes.

## Boundaries still open

No IPC transport, MCP helper target, setup UI, job persistence, media-inspection
adapter, or conversion executor integration is claimed by this increment. Existing
manual and App Intent paths have not yet moved onto the new registry. Those changes
remain gated by the signed/sandboxed transport feasibility work and focused
coexistence tests in the release plan.
