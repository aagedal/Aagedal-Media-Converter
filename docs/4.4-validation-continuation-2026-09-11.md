# 4.4 continuation validation — 2026-09-11

Scope: stabilization changes based on `4bb9cb7`, with license consistency checks
committed as `a1a52f1`. Development version remains **4.4.0 (576)**; this is not a
frozen or signed distribution candidate. Host: Apple Silicon, macOS 27.0 (26A428),
Xcode 26.6 (17F113).

## Changes under validation

- IMF export rejection before tool lookup, probing and output allocation, with
  explanations in Settings and errors, translated into Norwegian.
- Deno extraction/update and yt-dlp whole-update ownership and draining.
- Cross-process subtitle publication and same-destination upload locking.
- FFmpeg reported-license consistency checks alongside incomplete attribution.
- A progress cancellation test fixture now waits for its queued callback before
  ending its fake encoder process.

## Completed checks

- Full Debug/unit suite: **843 passed, zero failed, zero skipped**, including all
  four new updater regressions, three upload lease tests, IMF rejection/preservation
  tests, and the corrected callback and upload fixtures.

- Release-script suite: **53 passed**, including five license consistency tests
  two cross-process subtitle publication tests, and three cross-process upload
  lease tests.
- Conversion UI smoke: **3 passed** from a fresh locally signed build: success,
  error details and start/cancel.
- Unsigned Release build and bundle audit passed: **44 arm64 Mach-O images**,
  resolved bundled dependencies, and **six packaged notices** matching the manifest.
  This is packaging evidence, not signed-distribution or complete-attribution evidence.
- Localization: **1,533 entries**, only the 15 intentional untranslated tokens;
  placeholder and coverage checks pass.
- Inventory freshness passes; the strict license gate correctly fails with the
  existing 99 missing attributions and both FFmpeg license/notice mismatches.

An initial passing app suite did not include the two new updater test files because
this project requires explicit target registration. They were added to the Xcode
project. The next full run executed **843 tests: 842 passed, one failed**. All new
updater and remote-lock tests passed. The failing existing progress-cancellation
fixture allowed its fake process to exit before an asynchronous callback ran;
explicit bounded synchronization fixes that race without changing production
progress behavior. A subsequent run also passed 842/843: two existing upload
fixture classes ran in separate processes with the same fake destination, so the
new lease correctly rejected one. Each XCTest instance now uses a unique remote
directory; production locking and timeouts are unchanged. Swift concurrency
checking then required explicit value captures in those fixture tasks; these
were corrected before the final rerun.

## Evidence paths

Temporary local artifacts, not committed fixtures:

- `/private/tmp/aagedal-44-cont-script-final.log`
- `/private/tmp/aagedal-44-cont-license-gate.log`
- `/private/tmp/aagedal-44-cont-ui.xcresult`
- `/private/tmp/aagedal-44-cont-ui.log`
- `/private/tmp/aagedal-44-cont-final-unit.xcresult` (initial 842/843 run)
- `/private/tmp/aagedal-44-cont-green-unit.xcresult` (fixture destination collision)
- `/private/tmp/aagedal-44-cont-verified-unit.xcresult` (fixture compile diagnostics)
- `/private/tmp/aagedal-44-cont-verified-unit.log`
- `/private/tmp/aagedal-44-cont-passed-unit.xcresult` (final rerun)
- `/private/tmp/aagedal-44-cont-passed-unit.log`
- `/private/tmp/aagedal-44-cont-release.log`
- `/private/tmp/aagedal-44-cont-release-audit.log`
- `/private/tmp/aagedal-44-cont-footprint.json`

## Remaining release gates

No binary, license notice, appcast or public release was changed. Missing dependency
source/build records and attribution remain publishing blockers. The IMF restriction
is a release disposition, not evidence of a conformant exporter. Helper and lock
regressions verify bounded local ownership; they do not replace live network/model,
preview, sandbox renewal, recording/editor sync, Shortcuts, upgrade, accessibility,
clean-install or signed/notarized distribution checks in the release plan.
