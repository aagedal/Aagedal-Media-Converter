# 4.5 stitching editor to export UI validation — 2026-09-23

The generated two-clip stitching fixture now has a focused UI check that opens
the editor, ripples the first clip's in-point from the timeline, closes the
editor, starts conversion from the main toolbar, and waits for the merged-output
control to appear. The control has a stable accessibility identifier, so the
test checks the completed group output rather than inferring success from the
conversion button alone.

The focused `StitchingTimelineTests` and `TimelineKeyframeServiceTests` suites
passed all 47 cases, and the Backspace/undo UI test passed. Log:
`/private/tmp/amc-45-sept23-stitching-continuation.log`.
The new editor-to-export UI test passed. Log:
`/private/tmp/amc-45-editor-export-ui.log`.

This is a generated-source, in-app completion check. It does not inspect the
export's decoded frames, audio, or cut accuracy. Broader real-source playback,
MXF/card import, and decoder-loading stress remain release validation work.
