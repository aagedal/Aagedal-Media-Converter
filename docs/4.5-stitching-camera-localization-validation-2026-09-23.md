# 4.5 stitching and camera-card localization check

The stitching editor now localizes its sequence-timecode help and marker note help and accessibility label. Norwegian Bokmål entries were added for these strings and the previously untranslated timeline help, range controls, and shortcut guidance. The camera-card import and recording review strings already have Norwegian entries; no copy change was needed there.

Validation on 2026-09-23:

- Parsed `Localizable.xcstrings` as JSON and confirmed all 14 added or completed keys have translated `nb` string units.
- Audited direct user-facing literals in `StitchingEditorView.swift` and `CameraCardImportView.swift` against the catalog. No unmatched direct UI literals remain; system image names, format examples, and translator comments were excluded.
- `git diff --check` passed for the localization changes.

The parent task's focused Xcode build compiled these changes. Its camera-card
review UI runner hung before establishing a test connection, so the interaction
and Norwegian visual checks remain open.
