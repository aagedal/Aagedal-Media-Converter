# 4.4 post-release evidence audit — 2026-09-12

## Release identity

- Release tag `4.4.0` points to `3a35727`.
- The published appcast entry records version **4.4.0 (576)**, a Sparkle signature,
  and the release archive URL in commit `57c6c22`.
- Commit `89e5f2e` merged that appcast entry to `main`.

This establishes that 4.4.0 was published. It does not, by itself, recreate
checks whose output was not retained in the repository.

## Evidence that is retained

- The final automated candidate record documents 869 unit tests, 21 UI tests,
  74 release-script tests, localization and license gates, and an unsigned Release
  bundle audit at 4.4.0 (576).
- The release script requires code-signature, hardened-runtime, notarization,
  stapling, Sparkle-signature, extracted-archive, and Gatekeeper checks before it
  uploads the release and writes the appcast candidate.
- The committed appcast has a 4.4.0 Sparkle signature and matching version/build.

The latter two points strongly indicate that the scripted distribution checks ran,
but the repository does not retain the release invocation log, notarization result,
Gatekeeper output, or extracted-archive audit report. They therefore do not justify
retroactively checking those evidence boxes.

## Gates with no retained completion record

- The bounded hands-on conversion matrix covering trim/crop, timecode,
  multichannel audio, collisions, cancellation, and output inspection.
- Shortcuts open/closed live checks; output/watch-folder grant renewal; real-model
  transcription; live download/upload retry and cancellation; live recording and
  editor sync; VoiceOver; and an installed 4.3-to-4.4 saved-state upgrade.
- Clean direct-download installation, Homebrew installation, and a controlled
  update from 4.3.
- A final signed/notarized distribution report capturing the exact artifact hash,
  notarization identity, Gatekeeper result, and Sparkle verification output.

The candidate freeze and release-note/version work clearly occurred, but the
historical plan was never reconciled after publication. Its boxes remain unchanged
so the record does not pretend that undocumented checks were performed.

## Carry-forward correction

The disabled IMF export message named 4.4 specifically even though conformance is
still deferred for 4.5. Development now uses release-neutral wording in both the
Settings explanation and conversion failure, with the Norwegian translation and
regression assertion updated. IMF export remains disabled; no capability claim was
expanded.

## 4.5 release-process action

Before publishing 4.5, retain a release report from the signed artifact and record
the smaller hands-on acceptance matrix before updating the public feed. Treat
release-script success and live workflow evidence as separate records.
