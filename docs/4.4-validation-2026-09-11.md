# 4.4 stabilization validation — 2026-09-11

Scope: the release-preparation commit containing this record, based on `e37bed8`.
Development version **4.4.0 (576)**; not a frozen distribution candidate.
Host: Apple Silicon MacBook Pro, macOS 27.0 (26A428), Xcode 26.6 (17F113).

## Completed checks

- Debug build passed.
- Full unit suite: **837 passed, zero failed, zero skipped**. Eight new tests cover
  numeric-filter ambiguity and mapping changes, Matroska metadata labels, and
  positional AVC-Intra splitting. The existing AV2 unsupported-video regression
  now exercises synthesized video and both waveform engines.
- Generated 3.0 and 5.1(side) Matroska inputs retain every channel's sample values
  after the AVC-Intra split graph; all eight mono output tracks, including silent
  padding, are checked. Existing multichannel downmix and audio-first numeric
  crop pixel checks pass.
- Conversion UI smoke: **3 passed** from a fresh locally signed build—successful
  conversion, failure details, and start/cancel.
- Release-script suite: **43 passed**, using Xcode's SDK explicitly for the fixture
  compiler.
- Localization: **1,530 catalog entries**, only the 15 intentional untranslated
  tokens remain; placeholders pass.
- Unsigned Release build passed. The bundle reports **4.4.0 (576)**, and its
  audit verifies **44 arm64 Mach-O images** with resolved bundled dependencies
  and **six packaged notices** matching the manifest. This is a packaging check,
  not complete attribution or signed-distribution validation.
- Bundled inventory freshness and `git diff --check` pass.
- Draft 4.4 notes render through `scripts/changelog-to-html.py`; all historical
  sections beginning with 4.3 exactly match release commit `ce2f016`.

The initial sandboxed Xcode run could not write package caches. The permitted
Xcode run caught a mapped/raw metadata type mismatch, and test compilation caught
an ambiguous metadata import. Both were fixed before the final suite. Its first
completed run had one outdated assertion expecting Matroska's inferred 5.1 label;
the corrected assertion expects unknown speaker positions and retains the actual
conversion/stereo-output checks. The final complete run passes.

## Local evidence

- `/private/tmp/aagedal-44-debug-build.log`
- `/private/tmp/aagedal-44-unit-tests-green.xcresult`
- `/private/tmp/aagedal-44-unit-tests-green.log`
- `/private/tmp/aagedal-44-unit-summary.json`
- `/private/tmp/aagedal-44-ui-smoke.xcresult`
- `/private/tmp/aagedal-44-ui-smoke.log`
- `/private/tmp/aagedal-44-script-tests.log`
- `/private/tmp/aagedal-44-license-gate.log`
- `/private/tmp/aagedal-44-release-build.log`
- `/private/tmp/aagedal-44-release-audit.log`
- `/private/tmp/aagedal-44-release-footprint.json`

These temporary paths are local run artifacts, not committed fixtures.

## Open release gates

The strict complete-license gate still fails: 99 entries lack attribution.
The [provenance review](4.4-dependency-provenance-review.md) also establishes a
FFmpeg GPL-version mismatch outside that missing-entry count. No bundled binary,
license assignment, packaged notice, appcast, or public release was changed.

The remaining IMF descriptor/conformance, multi-process subtitle/remote
coordination, and package/helper-draining dispositions are still open. This run
does not replace live preview, Shortcuts, sandbox renewal, real-model transcription,
remote transfer, recording/editor sync, VoiceOver, bilingual UI, upgrade, clean
installation, or signed/notarized distribution validation. See the
[release plan](RELEASE-4.4-PLAN.md) for the complete finite checklist.
