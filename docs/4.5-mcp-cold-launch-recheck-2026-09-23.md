# 4.5 MCP cold-launch recheck — 2026-09-23

This recheck used macOS 27.0 and the installed `/Applications/Aagedal Media
Converter.app` version 4.5.0, build 577. The embedded helper SHA-256 was
`0a98d1de2f735317f66a3af1ea81ac42d5115942463bc18975805f95a2d55f65`,
the same binary as in the earlier
[named-client validation](4.5-named-client-validation-2026-09-23.md).
The installed app's `localAgentAccessEnabled` preference was `0`. This run did
not change that preference or any MCP client configuration.

## Result

A direct stdio exchange with the installed helper negotiated MCP `2025-06-18`.
The subsequent `list_presets` request with `{}` returned `isError: true` and
`structuredContent.error.code: transport_unavailable`, with the message
`Aagedal Media Converter could not be launched.` The process exited normally
after about 3.5 seconds. This happened under the restricted command sandbox;
it does not establish an app launch defect in a normal login session. It also
does not count as a named Codex tool call or a successful preset response.

The seven process-level helper framing and discovery tests passed against a
freshly compiled helper (`python3 -m unittest scripts.tests.test_mcp_helper -v`).
They exercise initialization and tool discovery, not the live app endpoint.

Current source has the newer bounded launch/endpoint polling logic described in
the [helper launch poll record](4.5-helper-launch-poll-validation-2026-09-23.md).
The installed build 577 predates that change. A new packaged build with Agent
Access enabled is still required for the successful named-client `list_presets`
and helper-triggered cold-launch acceptance checks. The full named-client
conversion, reconnect, cancellation, and restart matrix remains open.
