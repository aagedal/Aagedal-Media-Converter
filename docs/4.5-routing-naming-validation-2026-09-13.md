# 4.5 manual routing and naming validation — 2026-09-13

## Implemented scope

- Extended the backward-compatible per-source execution snapshot with optional
  audio routing and output base-name overrides. Older schema-v1 requests and
  persisted jobs continue to decode with both additions absent.
- Routed otherwise-supported manual H.264, HEVC, ProRes, Proxy, and Audio Only
  rows with audio track selection, ordering, duplication, downmix, merge, split,
  swap, or channel extraction through the shared planner and serialized executor.
  Stream Copy retains the legacy path because it cannot apply audio filters.
- Validated unique non-negative input stream identities, output track identity
  and membership, channel-operation arity and bounds, and preset compatibility
  before planning. MCA label overrides stay outside this six-preset contract.
- Captured custom output base names, constrained them to a bounded path-safe
  component, and rendered them with the accepted filename rules. Custom names
  now participate in the planner's duplicate-output, existing-file warning, and
  submit-time collision behavior instead of being recomputed by the legacy path.
- Passed each accepted routing snapshot to the matching FFmpeg conversion and
  exposed routing and custom naming in the immutable per-row settings inspector.

## Automated validation

Focused application contract and handoff suites: **64 passed, 0 failed, 0
skipped** on macOS 27.0 with Xcode 27.0.

Full unit target: **923 passed, 0 failed, 0 skipped**. The result contains the
same four existing Thread Performance Checker QoS priority-inversion warnings
recorded by the preceding 4.5 validation runs.

The focused suite verifies backward decoding, request round trips, bridge
capture, unsafe routing/name rejection, custom-name output planning, and exact
FFmpeg handoff. `git diff --check` passes.

## Remaining boundary

Generated waveform behavior, uploads, subtitle generation, analytics,
groups/merge, and per-source output destinations remain outside the shared
contract. Unsupported and per-source-destination Shortcut submissions also keep
their established executor.

Beta validation still requires real routed-output playback and channel-layout
inspection, custom-name collision checks through the UI, concurrent manual and
agent cancellation, two named MCP clients, cold launch/reconnect/restart,
permission-loss cases, Norwegian localization and visual review, and a Developer
ID Release archive with nested-code verification and notarization.
