# 4.5 card, MPV preparation, and release checks — 2026-09-22

## Completed

The camera-card review keeps clips from each directory together and checks
marked multi-file recordings independently of their whole proposed group. The
retained GPL-enabled MPV candidate can be prepared under the expected
`Libmpv-GPL.xcframework.zip` asset name with matching bytes and a recorded
SwiftPM checksum. The asset remains unpublished and is not used by this app
build. The previously removed `Package.resolved` pins were restored so the
dependency manifest has a reviewable, fixed input.

## Validation

- All **1,178 Debug unit tests passed**, with no failures or skips. Result:
  `/private/tmp/amc-45-final-unit-approved.xcresult`; build log:
  `/private/tmp/amc-45-final-unit-approved.log`. Four previously recorded
  test-target QoS warnings remain.
- All **98 release-script tests passed**. The Norwegian catalog audit passed
  with 1,761 entries, 19 intentional untranslated tokens, and no unclassified
  missing keys or placeholder mismatches.
- The strict bundled-dependency manifest check passed after restoring the
  committed package pins.
- The unsigned Release build passed. Log:
  `/private/tmp/amc-45-final-release.log`. The static bundle audit verified
  45 arm64 Mach-O images, bundled library resolution, and 12 packaged license
  notices. Report: `/private/tmp/amc-45-final-release-bundle-report.json`.
- All 20 focused card-grouping tests and 17 focused MPV script tests passed;
  see the separate card and MPV validation records for fixture and artifact
  details.

## Limits and release gates

The Xcode 27 test invocation with `-onlyUsePackageVersionsFromResolvedFile`
removed the restored workspace `Package.resolved` during resolution, then
failed because automatic resolution was disabled. A normal build using the
cached package source completed; the committed pin file was restored again
after Xcode finished. The release script's strict package-resolution/archive
path therefore still needs a successful run before distribution.

The Release app was built with `CODE_SIGNING_ALLOWED=NO`; this is not a signed
or notarized distribution candidate. This host reports zero valid code-signing
identities. MPV publication/integration and affected-macOS CoreAudio recovery,
physical-card and real-span checks, named-client workflows, bilingual visual
review, and clean installation/update validation remain open.
