# 4.5 folder-authorization validation — 2026-09-13

## Scope

- Continued the shared job boundary after persisted plans and job history.
- Required a persisted security-scoped read grant for every source and a
  persisted writable grant for the destination before planning or submission.
- Reused the app's existing bookmark store and selected the narrowest matching
  exact-file or ancestor-folder grant.
- Resolved symlinks before containment checks so a path cannot escape an approved
  folder through a symlink.
- Retained and balanced all source and destination scopes through identity,
  destination, and collision validation.
- Rechecked every grant at submission so a plan cannot enqueue work after its
  source or destination access is revoked.
- Added stable `source_access_denied`, `destination_access_denied`, and
  `destination_unavailable` error codes.

## Results

- Focused job-contract and bookmark suites: **40 passed**, no failures or skips.
- Full unit target: **897 passed**, no failures or skips.
- The full run reported the existing QoS priority-inversion runtime warnings in
  `Aagedal_Media_Converter_Tests.swift`.
- The app and test targets compile under Swift 6 strict concurrency.
- `git diff --check` passes.

## Boundaries still open

The app does not yet provide dedicated Agent Access folder-grant settings, so the
service currently recognizes compatible grants already saved by existing app
workflows. Conversion execution from captured settings, UI/App Intent routing,
queue origin labels, the local MCP transport, setup diagnostics, and signed-helper
feasibility remain subsequent work. Denied and revoked real-world sandbox cases
still need manual validation in a signed build.
