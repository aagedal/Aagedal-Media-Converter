# 4.5 bilingual Settings UI validation — 2026-09-23

The focused `testMainWindowAndEverySettingsPaneInBothLanguages` run initially
failed before opening Settings. A fresh UI-test launch displayed the first-open
anonymous usage choice, which correctly blocked interaction with the main
window. The UI-test launch helper now explicitly chooses **Don't send data**
when that sheet appears. The decline button has a stable accessibility ID so
the same setup works in English and Norwegian.

After this change, the focused UI test passed in both languages: one test, zero
failures. It selected all 19 Settings panes in each locale, checked their
localized sidebar labels, and retained 40 screenshots of the main window and
pane states. The Norwegian General and Agent Access screenshots were visually
spot checked; their visible labels and controls are readable, with additional
content available by scrolling.

The Norwegian catalog audit also passed: 1,761 entries, 19 intentional tokens,
no unclassified missing keys or placeholder mismatches.

Local evidence: `/private/tmp/amc-45-locale-ui-fixed.log`,
`/private/tmp/amc-45-locale-ui-fixed.xcresult`, and exported screenshots in
`/private/tmp/amc-45-locale-fixed-attachments/`.

This covers Settings pane navigation and a limited visual spot check. The
stitching editor, camera-card review, and other new 4.5 panels still need
bilingual visual inspection.
